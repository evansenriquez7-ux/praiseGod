# PG Pipeline Contract

A generator is done when `python -m backend.app.practice_gen.validation.run_all` exits 0 and the judgment review (`pgen_judgment.md`) is filed. Checking a box proves nothing; the command output proves everything. If you believe a rule here is wrong, change the harness and this table in the same PR — never quietly deviate.

## Contract Rules Table

| Rule | Enforced by | Runs in |
|---|---|---|
| Scalar 0.0/1.0 map exactly to competency bounds — **exempt for the `number_difficulty` axis**, whose mapped value IS the scalar, making the assertion vacuous. 82 of 151 nodes carry it as their only continuous axis and therefore have no §1A; §1H asserts that the remaining 61 all do | `validate_matrix` §1A | local `run_all`; not in CI |
| No leaky windows; monotonic windows | `validate_matrix` §1B | local `run_all`; not in CI |
| Scalar 1.0 actually reaches the competency's stated range | `validate_matrix` §1A-reach | local `run_all`; not in CI |
| Every supported variant×formatter executes cleanly with valid answers | `validate_matrix` §1C | local `run_all`; not in CI |
| Unsupported combos raise; no silent substitution | `validate_matrix` §1C-reverse | local `run_all`; not in CI |
| No NOT_YET_KNOWN vocab in formatted output. The independent full-node audit now executes **every mapped DNA**, including secondary mappings, on the student context path; the static `VocabGated` lint likewise audits a DNA against every node that maps it rather than `concepts[0]`. Both directions have detected mutations. BLIND SPOT: the full-node audit stops at `QuestionContext`; §1D remains the final-formatter check, and the H-04 release executor has not completed its six shards | `validate_matrix` §1D; `validate_vocab` `vocab_audit_pass_rate`, `static_vocab_gated_lint` | local `run_all`; not in CI |
| Answer key survives formatting; interest-invariant. Interest validation runs the final student path for every applicable node/DNA, every grade-supported theme, and five deterministic seeds. A requested theme must survive in metadata **and** contribute a declared theme-bank value to the final learner-facing problem; metadata alone is not delivery. The shared generator adds a short grade-gated thematic cue only when the selected story/question used no resolved interest slot, the orchestrator preserves that cue when a formatter rebuilds the question, and `emoji_pictorial` uses the bank's declared emoji instead of an unrelated random symbol. Measured clean: 94/94 node/DNA pairs and all 4,100 node/DNA/theme/seed requests visible. Both mathematical invariance and post-formatter theme loss have detected mutations. BLIND SPOT: presence does not prove the narrative is natural, helpful, or coherent; that remains judgment work | `validate_matrix` §1E; `validate_interest` `interest_invariance`, `interest_theme_visibility` | local `run_all`; not in CI |
| A question stem never gives away its own answer. TWO independent paths: (a) the answer is the stem's sole numeric datum, so it can be copied rather than derived; (b) the stem DECLARES it — "it is X", "the answer is X" — regardless of what else the stem carries. Path (b) closes the hole path (a) left by design, without reopening the 3,702 false positives the naive wide form produced; verified silent across 906 renders before being asserted. Worked examples ("for example 45 = 40 + 5") are exempt | `validate_matrix` §1F | local `run_all`; not in CI |
| Every knowledge-graph node generates: the matrix enumerates nodes independently from `NODE_TO_DNA`, so deleting a mapping is a named `NODE_TO_DNA_presence` failure instead of silently shrinking the sweep. No node passes on an empty execution matrix — scoped to formatter/DNA pairs the node can actually be served. A pair whose competency bounds refuse it (ALL bound values must be renderable, not any) is covered by §1C-reverse's "must raise", not here. Before 2026-08-26 the refusal test skipped list-valued bounds (78 exist), so `mat_g3_na_q3_1` reported `empty_execution_matrix` against `array_grid_read`, a formatter it never offers | `validate_matrix` §1C-coverage | local `run_all`; not in CI |
| Registry/compatibility coverage is bidirectional in every relation: KG↔`NODE_TO_DNA`↔`COMPATIBILITY`↔`DNA_MODULE_MAP`; unused/orphan declarations fail. KG monotonicity follows every explicit `prior_node_ids` edge (5,726 currently), never adjacency inferred from a G1–3 filename sort. Lab v2 and the student portal expose pure generation seams used by their live endpoints; `lab_portal_equivalence` executes both with identical inputs and compares every output field for one manifest-derived obligation per current node/DNA/formatter route. All three gates have live-path detected mutations. BLIND SPOT: the fast equivalence sweep uses the first reachable discrete assignment per route; H-04 owns the complete product. Database configuration selection and legacy `QuestionResponse` serialization are outside this assertion | `validate_compat` §2 | local `run_all`; not in CI |
| Difficulty profiles meet `MIN_ACCEPTANCE_RATE`; DNA structure exercises every applicable node at every declared grade and five deterministic seeds through production `generate_context`, parses multi-digit grade keys, compares numeric values exactly with `Fraction` rather than float tolerance, and hard-fails any produced distractor equal to the key. Grade bounds without a mapped node are syntax-sampled directly and are explicitly not claimed as student-path coverage. Equal-valued wrong representations remain §1K's display-aware domain. Both structural and feasibility labels have detected mutations | `validate_dna` §3 | local `run_all`; not in CI |
| Response payload matches strict schema | Pydantic model (runtime, every problem) + `validate_matrix` §4 (**visual payloads only** — §4 is recorded solely under `is_visual`, so it covers 67 of 151 nodes; non-visual response shape rests on the Pydantic model alone) | runtime + local `run_all` |
| Every node carries a genuine, non-boilerplate, current blind judgment review with a PASS verdict. Schema v2 binds the exact competency and requirements, complete canonical student-path samples (including ordered options, resolved answers, response configuration and render-derived visual evidence), replay/packet digests, per-sample assessments and exact clause coverage. Dispatch provenance binds raw-response bytes by digest; each sample/clause reviewer must equal its attributed dispatch reviewer, and each dispatch’s clause allocation must exactly match its attributed clause evidence. Negative controls use seed 42. **Limitations:** identity/bytes alone do not establish reviewer independence or prove that filed reasoning equals the raw reply; the merged lossless filing workflow and fresh live reviews remain open. Static visual evidence does not establish physical layout or pointer geometry | `validate_judgment` §5 (`judgment_dispatch_provenance_5` for attribution/allocation) | local `run_all` **Phase 2**; not in CI |
| Every node declares what its MATATAG competency requires, cites the clause, covers every competency word, and the pipeline provides it. **Phase 1 (artifact-free) since 2026-09-08.** The phase boundary is not "does this check need an LLM" — every §6 check is programmatic, and the LLM only ever *produces* an attestation — it is whether a check needs an agent-authored artifact to exist before it can run. This band reads the knowledge graph, the `requires` declarations and `CAPABILITY_PROVIDERS` and nothing else: 75 findings in 0.1s with `validation_reports/attestation/` absent, against 90 in 9.9s for the attestation band it used to share a boolean with. Two gates make the seam mechanical rather than conventional: `capability_phase_boundary_6` re-runs Phase 1 with the corpus absent and requires byte-identical findings, and `capability_phase_partition_6` refuses a finding citing a section ref registered to the other phase (`validate_capability.CHECK_PHASE` is the registry, imported by `run_all` rather than copied). BLIND SPOTS: the boundary gate is behavioural, so a Phase-1 read that changes no finding today is a latent dependency it cannot see (a static AST pass would close it); and the partition gate cannot see provenance/coverage/provision findings, whose messages cite no section ref of their own Its provenance, coverage, provision and bounds-discrimination checks were UNPROVEN until 2026-09-09 while 75 findings sat under them (Mandate 3); all four now have named mutations, and so does a new one: the declarations this band validates must match `data/skeletons/vocab_annotation.json`, the hand-authored source that `scripts/rebuild_knowledge_graph.py` builds the graph from — nothing checked they agreed, so an author who skipped the rebuild had §6 validate a stale copy indefinitely. That gate compared `requires`/`requires_ignore` only and the graph checked in at 8cb8dd22 was ALREADY stale one field over — 18 nodes carried a `cumulative_concepts` entry a fresh build does not produce, which is the ground truth §1D's NOT_YET_KNOWN gating reads. It now rebuilds the skeleton in memory and compares EVERY field, because a hand-picked field list goes stale the moment the builder derives something new. **Floor 5 since 2026-09-09** (was 75, unfloored). The 70 cleared were registration, not capability: 45 provider entries named only the generic textual family while the DNA's own discriminating variant existed and rendered the clause (each verified by rendering before registering). Three DNAs were declaring variants nothing honoured — `ordinal_numbers` named two task types no template carries and silently fell back, so `describe_position` was BUILT because every ordinal competency reads "the position of objects" and no item contained one; `order_of_operations` and `probability_experiment` read variants they never declared. The residual 5 are one capability — a DRAWING SURFACE, which no formatter in the tree has; `draw_construct` renders MCQs *about* drawing and a blind Attester ruled it NOT_PROVIDED, so registering it would manufacture a CONTRADICTED. The floor sits AT the count, so a sixth fails immediately. A competency word may be waived only by a HUMAN: `requires_ignore` is ground truth under `docs/pgen_rulings.md` R-5, its sanctioned contents are locked in `data/skeletons/requires_ignore.lock.json`, and any divergence fails in either direction — otherwise the coverage check's own remedy ("declare the requirement, or list them in requires_ignore") lets an agent silence a finding by waiving the competency instead of serving it. The lock makes that a separate auditable commit; it cannot make it impossible. The requirement direction is now checked BOTH ways (2026-09-10): a `CAPABILITY_PROVIDERS` entry no node's `requires` names is a finding with **no floor**, because every other §6 check is driven by a requirement — an orphan is read by none of them and stands ready to pre-approve the first future node whose competency extracts that id, unattested. `draw_lines` was in that state, orphaned by a rename when `draw_line_relationships` was unregistered on an Attester ruling; count is now 0. A competency word dropped from `requires` on a documented ground-truth correction goes in `requires_ignore` (Protocol 5), and the node's `requires_ignore_note` records the ruling — that note is **not validated by anything**. | `validate_capability` §6 | local `run_all` **Phase 1**, stage 8/9; not in CI |
| A capability's provider is the artifact that renders what the clause names, never a generic textual formatter every DNA already offers. **Phase 1**: 74 of the 75 artifact-free findings are this rule, and they were filed in the Phase 2 backlog until 2026-09-08. They MOVED, they did not shrink | `validate_capability` §6D | local `run_all` **Phase 1**; not in CI |
| Every node id referenced anywhere in `backend/app/` must exist in the registry. `placement.py`'s forward-looking G4–G10 milestone ladder holds 7 that do not (latent — it is called from nowhere), so the floor is 1 file; any NEW file naming absent nodes fails | `validate_compat` §2F | local `run_all`; not in CI |
| Every node's competency bounds parse to a well-formed shape — a (min,max) tuple, a non-empty value list, or a scalar sentinel — asserted TREE-WIDE rather than against the ~20-row G1–G3 fixture table, so a new grade inherits the property automatically (Mandate #4) | `validate_compat` §2G | local `run_all`; not in CI |
| A competency naming BOTH cases of a dimension ("with and/or without X", matched as a pattern so grade 4–10 wordings are caught too) must not have X bound to one case. §2G proves bounds are well-FORMED; this proves they are FAITHFUL. Until 2026-09-04 `"without regrouping" in text` matched "with and without regrouping" as a substring, pinning mat_g3_na_q2_1 and mat_g2_na_q1_9 to 0/120 carries with every stage green — narrowing a bound removes items, so nothing downstream complains. Blind spot: a clause the parser ignores ENTIRELY emits no bound and is invisible here (that is §6's job) | `validate_compat` §2H | local `run_all`; not in CI |
| A discrete variant a node DECLARES must be one it can actually produce. **Floor 0 since 2026-09-08** (was 65). The last 12 split both ways: 10 were values `CURRICULUM_VARIANT_GATES` already refuses at the node's grade/quarter (expanded_form and associative on G1 addition, multi_digit on G2 multiplication, draw_construct/recognize_model on a G2 "identify and explain" geometry node) — the packet builder now CALLS `is_variant_available_at` instead of declaring past it, which also retired the hand-mirrored `_unit_type_applies`; 2 were the opposite, regrouping='four_places' on "sums up to 10 000" nodes the orchestrator capped at 9999 because `int(pow(10, …))` truncates — building it was the fix. §7's `variant_candidates` floor (974→964) guards against clearing these by over-filtering | `validate_compat` §2I | local `run_all`; not in CI |
| A formatter a node advertises must be REACHABLE by the student path, not merely servable when pinned (§2B). Counted in **(node, formatter) pairs since 2026-09-08 — floor 35** (37 until 2026-09-11, when the tree was measured at 35 both before and after that day's content work: two pairs of headroom nobody had ratcheted away), because the node count (floor 18) could never see a node already failing lose a SECOND formatter, and a floor defeats only a regression smaller than its headroom. The matrix stage validates content no pupil receives and the Lab menu is wider than what is served. BLIND SPOT: 40 seeds cannot separate "never offered" from "offered and not drawn" — 36 of the 37 are hard `FORMATTER_VARIANT_SUPPORT` exclusions (the node over-advertises for its own competency), the 37th (mat_g2_na_q3_5/array_grid_read) is served 8 times in 400 seeds and is a sampling miss | `validate_compat` §2C | local `run_all`; not in CI |
| The correct option's position must not be predictable from the seed — measured across nodes at a fixed seed, no slot may exceed 45%. Before 2026-08-28 it was 93% (slot 1 at seed 11), so a pupil could score ~90% by always picking one position | `validate_compat` §2E | local `run_all`; not in CI |
| A saved Lab configuration may not serve content outside a node's competency — `allowed_difficulties` / `allowed_contexts` are intersected with the node's CURRENT bounds and an empty intersection raises. A config legal when saved is not legal forever. Value SETS only; numeric (min,max) bounds stay with §1A/§1B | `validate_compat` §2D | local `run_all`; not in CI |
| A formatter a node advertises must actually be servable for that node — the advertised list may not promise what the orchestrator refuses | `validate_compat` §2B | local `run_all`; not in CI |
| A true/false item family may not key every sample the same way — an all-True property set lets a pupil score 100% by answering yes, so it measures nothing. Boolean answers only, pooled across a (DNA, formatter) pair's variant combinations | `validate_matrix` §1I | local `run_all`; not in CI |
| An explicit count and the noun after it must agree in the text a pupil READS — the rendered stem, its nested quoted statements, its options and its hint. The rule is the generator's own (`dna.base.to_singular_phrase`), imported rather than restated, so the harness and the pipeline cannot disagree about what correct English is. **Floor 0 since 2026-09-12**, measured over 9060 student-path samples (60 seeds × 151 nodes) after four live stems were fixed at their composition sites — `jump back 1 units`, `move back 1 steps`, `Taking away 1 cookies`, `Shade 1 groups of 1 squares` — and after the shared inflection rule was fixed to stop mangling singulars ending in `-us` (`1 hibiscus` → `1 hibiscu`). BLIND SPOTS: the mirror direction (a singular noun after a count of two or more) is MEASURED and printed but does not gate, because deciding the head of a noun phrase needs more than the next token; count/verb agreement is not checked at all; and a noun outside the pattern's reach is counted as unjudged rather than assumed correct | `validate_language` §1J | local `run_all`; not in CI |
| A choice item must offer choices a pupil can tell apart, and exactly one of them may answer the question: no two options rendering the same text, no second option carrying the keyed answer's exact typed value, no two options flagged `is_correct`. Values compare exactly and by type (`Fraction`, never float) with the unit KEPT, so `5 cm` and `5 m` are different answers. **Floor 0 since 2026-09-12**, measured over 1180 option-bearing student-path samples. DELIBERATELY NOT GATED: two DISTRACTORS of equal value (7 observed — `1/2` beside `2/4`, `1/10` beside `2/20`); a representation task may legitimately offer them and neither touches its key, so banning them would be the gate inventing a rule the curriculum does not state. The count is printed on every run. BLIND SPOT: an equivalence domain this gate cannot parse (`3:30` vs `half past three`, two coin sets making the same amount) is counted as unjudged and stays judgment-review work; it is never silently covered by a widened guess | `validate_options` §1K | local `run_all`; not in CI |
| Every check a node's own composition makes applicable actually ran **on that node** — the suite-level union proves a check ran somewhere, not everywhere | `validate_matrix` §1H | local `run_all`; not in CI |
| A known-correct answer must be graded CORRECT by all three graders (portal, Lab v1, Lab v2). **Floor 0 since 2026-09-08** (was 5, all real: an unrecognised format fell through to an MCQ *key* comparison in portal/lab_v1, so `sort_order`'s [10,9,8] was tested against "A"; lab_v1 also parsed the correct answer more leniently than the student's). All three now share `services.scoring.answers_match`, keyed off the answer's shape, not a format-name list | `validate_grade` §10 | local `run_all`; not in CI |
| **The other direction, binding since 2026-09-12:** a KNOWN-WRONG answer and a MALFORMED submission must be graded INCORRECT by all three graders. Until this date §10 asserted acceptance only, so an **always-true grader passed it perfectly**. Measured 0 over 906 refusal submissions (151 nodes × 3 seeds × {wrong, malformed}) after two real defects were fixed at their root: (a) all three graders shared `str(v).lower() in ("true","yes","t","1")`, which has no failure value, so a gibberish submission became False and was graded CORRECT on every False-keyed `true_false` item — now one strict `services.scoring.parse_bool_answer`; (b) Lab v2's unrecognised-format fallback was a raw `str(a) == str(b)` — now `answers_match`, the same shape-keyed comparator the portal uses. The wrong answer is DERIVED from the key (another declared MCQ option, a swapped order, a perturbed field, ±1), which bounds it: a grader that accepts a *differently* wrong answer is not caught | `validate_grade` §10 `grading_refusal_10` | local `run_all`; not in CI |
| An equivalent rendering of the correct answer must still be ACCEPTED. **Whitespace only, and that boundary is deliberate.** Measured 0 over 256 padded submissions after the MCQ key comparison — hand-written seven times across the three graders, stripped by Lab v2 alone — became one `services.scoring.normalize_option_key`: before the fix, `"  B  "` against a key of `"B"` was refused by portal and Lab v1 and accepted by Lab v2 on **211 of 256** samples. **NOT GATED, measured and recorded:** decimal rendering. `"73.0"` against a key of `73` is refused on **122 of 122** whole-number fill-in-blank/cloze samples, by all three graders alike — no grader disagreement, only a shared strictness. Whether a MATATAG whole-number blank admits a decimal form is a curriculum ruling (Protocol 5), not a harness decision, and stays open | `validate_grade` §10 `grading_equivalence_10` | local `run_all`; not in CI |
| Every §10 (node, seed) obligation is executed or fails BY NAME, and the graded path reaches no external host. Until 2026-09-12 a generation crash and an underivable answer were both answered with `except: continue`, so a node whose grading contract was never exercised reported identically to one that passed. §10 also opened the CONFIGURED database and wrote a shared `GraderGate_shared` learner into it, so on 2026-09-12 the same code crashed Phase 1 on Neon DNS in the morning and passed in the afternoon — non-determinism in a gate (Protocol 6), plus production learner drift. It now runs inside `tests.hermetic_db.hermetic_database()`: throwaway SQLite, deleted on exit, every outbound non-loopback socket raising `HermeticNetworkError` by name. Measured consequence: the full tree fell from **14m23s to 33s**. BLIND SPOTS: the socket guard hooks `socket.socket.connect`/`connect_ex`/`create_connection` and allows loopback (TestClient needs it); SQLite is not PostgreSQL, so a Postgres-only construct would fail here rather than in production — the safe direction, and it is how the `Attempt.selected_answer` list-binding defect was found | `validate_grade` §10 `grading_obligation_10`, `grading_hermetic_10` | local `run_all`; not in CI |
| A node's payload must carry every key the React component reads — a missing `visual_params` key collapses to `undefined`/`0`/`false` and breaks the render. **Floor 0 since 2026-09-08** (was 16: 7 real, fixed in `fmt_fraction_model`; 9 were this check selecting on `visual_type`, the node's category, rather than on `is_visual` as both renderers do). Applies to problems that actually carry a payload  The key list is **derived from the component AST** (`tests/frontend/extract_visual_contract.mjs`, Babel, already a frontend dependency), never hand-written: the map it replaced described itself as "derived from VisualSkeletons.jsx" and had drifted 35 keys away from it — every key reached by `const {...} = params` destructuring was invisible, while `dot_value` was demanded of NumberLine although the component reads it as one arm of a three-way fallback. Reads are classified from the AST as required, required-GROUP (`a ?? b ?? c` — supply at least one, which is Bug #58's shape), conditional (branch-only), optional (has a real default) or unused. **Blind spot, named**: conditional keys are reported, not enforced — enforcing them means evaluating the guard, which would be a second model free to drift. §9 checks the unconditional keys; the auditor checks groups too, so §9 is the weaker of the pair. First payload to depend on one: the equal-jumps number line (2026-09-11), whose `jump_count`/`jump_size` are branch-read and therefore conditional — a unit test gates their absence and §1G their consistency. Measured the same day: on BOTH visual types that gained a payload, every unconditionally-read key is also required by the Pydantic model, so an omission fails at §4 before §9 sees it — §9 is stricter than nothing here, not stricter than §4. Baseline measured over 477 payloads across 16 visual types: 0 violations.| `validate_render` §9 | local `run_all`; not in CI |
| A current, digest-bound frontend artifact must prove that React actually executed every **production-reachable** visual type on real forced student-path payloads in both active and disabled modes, emitted a non-degenerate static tree, and produced a countable description parsed from that rendered markup. `run_all` only consumes the artifact; it never regenerates the evidence it judges. Measured 2026-09-14: 27 reachable formatter/payload classes cover 15 visual types and emit 54 render-derived descriptions. The equal-jumps NumberLine carries one `data-pgen-role="number-line-jump"` marker per declared hop, so a conditional-key branch disappearing is visible even though the schema contract cannot model its guard. **Not complete H-08 coverage:** `BalanceScale`, `Categorize`, `RuleDiscovery`, `SortOrder`, and `TenFrame` remain registered in `renderUtils` but no current practice student-path obligation produces them; the artifact names them as NOT COVERED rather than manufacturing fixtures. **Blind spots:** jsdom has no layout engine, so NumberLine and BarChart pointer-drag geometry is unproven; static markup cannot establish crowding, overlap, colour contrast, or touch-target size | `validate_render` §12 `frontend_artifact_fresh_12`, `frontend_static_render_12`, `rendered_visual_description_12` | explicit `PYTHONPATH=. .venv/bin/python tests/frontend_suite.py`, then local `run_all`; not in CI |
| Every production-reachable interactive visual route must be driven through a correct non-pointer UI interaction, and the value its `onAnswer` callback emits must be accepted against that problem's backend key by the shared `services.scoring.answers_match`. The consumed artifact carries exactly one active and one disabled render per case, and its round-trip case IDs exactly equal the interactive case IDs derived from those rendered cases; a nonempty subset is a failure. Measured 2026-09-14: 10 interactive routes clean after two live defects were exposed and fixed: ClockSet emitted `"09:45"` against `"9:45"`, and `peso_money_build` was offered to notation/comparison tasks whose string keys could never equal the numeric amount the component emits. The former now emits canonical H:MM; the latter is restricted to numeric-answer operations and the empirical exclusion map was regenerated. **Not claimed:** MCQ visuals do not emit the selected option—the surrounding question renderer owns that path—and the five registered-but-unreachable types named in the preceding row have no honest student-path round trip | `validate_render` §13 `frontend_answer_roundtrip_13` | explicit `npm --prefix frontend test`, then local `run_all`; not in CI |
| Every assertion the harness can emit is either proven by an EXECUTED mutation — a verified proof record in `validation_reports/mutation_proofs/` reporting DETECTED — or listed in `UNPROVEN_ASSERTIONS` with a reason and a date; the allowlist may only SHRINK. **2026-09-12: `Mutation.asserts` alone no longer counts.** A record is rejected if it is missing, partial, malformed, left half-written by an interrupted run, stale against the mutation's definition or against the working-tree digest of `mutation_proof.INPUT_ROOTS`, reports SURVIVED or INVALID, carries an expected marker that was not observed, cannot confirm the tree was restored, or is bound to paths outside the fingerprinted input set (`phase1_admissible: false`). `run_all` CONSUMES proofs and never invokes the runner; there is no flag that accepts a stale one. The inventory is the WHOLE harness: `validate_matrix`'s labels are read from its AST (f-string families included), every other module declares `ASSERTIONS`, and five further directions pin those declarations — a mutation may not assert an undeclared label, the allowlist may not name one, an allowlisted label a mutation now proves must come off, every `  FAIL <label>` a validator prints must be inventoried, and every check in run_all's CONTRACT_CHECKS must declare which harness PHASE it runs in (`_manifest.CHECK_PHASE`) — a ref with no phase is omitted from every `--phase` run while the two-direction tripwire still passes. 2026-09-08: 46/96 proven, 50 knowingly unproven (was 16/26 while 27 proven assertions sat outside the inventory entirely). BLIND SPOTS: a check that neither declares `ASSERTIONS` nor prints a `  FAIL` line; a second mutation on an already-proven label (§7's `mutations` floor guards that); and — the limit of execution-proof itself — a proof states only that the check FIRED on one planted violation, never that the check is correct, that the violation is the only shape of that defect, or (Mandate 2) whether a survivor means a broken check or a plant that no longer reaches the validated path | `validate_coverage` §8 | local `run_all`; not in CI |
| Every harness stage is DECLARED before anything runs, and each runs behind its own exception boundary, so a stage that raises cannot take the stages after it — nor the drift tripwire, nor the summary. **Measured 2026-09-12 by planting a raise in one live stage:** `run_all` returned `None` instead of an exit code, §10, §8 and §7 were never reached, the two-direction section and the final summary were never printed, and **no line in the output named the crash**. An operator saw a traceback; they did not see that the grading contract never ran, which is worse than a red run because a red run says which obligations were checked. The ledger records five states — `scheduled` (declared and never entered, a FAILURE), `attempted`, `completed`, `failed`, `crashed` — and prints them with timings. The distinction that matters: a **failed** stage's contract refs leave the two-direction comparison, because it ran and reported; a **crashed** or never-entered stage's refs STAY in the expected set, so the crash cannot silently delete the obligations it skipped. This also replaced six hand-written `if not X_ok: discard(...)` pairs that were wrong in both directions — §2C–§2I were never discarded with §2, and §1J/§1K/§9/§10/§8/§7 had no pairs at all. BLIND SPOT, named: the three mutations proving this drive `tests/unit/test_stage_ledger.py` against the real `StageLedger` and a stubbed `run_all`, not a live validator crash, because a full Phase 1 costs ~8 minutes and the Phase 2 baseline is red by construction. They prove the control flow; the live measurement above is what establishes that a real validator crash is contained | `run_all` `stage_ledger_complete`, `stage_crashed_` | local `run_all`; not in CI |
| Every ref a harness stage declares is registered to that stage's own phase in `_manifest.CHECK_PHASE`. These are two registries describing one fact — the manifest decides which band a ref BELONGS to, the stage schedule decides which band actually RUNS it — and nothing compared them until 2026-09-12. Move §9 to Phase 2 in the manifest and `render_contract_9` still runs in Phase 1, so `--phase 2` reports it registered-but-never-executed forever while `--phase 1` goes on executing a ref it does not own. A ref with no manifest entry at all is the same finding from the other side. NOT to be confused with `run_all`'s `misphased` reconciliation, which compares `executed_phase` against the very registry `_record_phase` derives it from: that branch is defensive and **unreachable by construction** while both sides share one source, and is recorded here as a limitation rather than proved by a contrived plant | `run_all` `stage_phase_matches_manifest` | local `run_all`; not in CI |
| The student-path obligation manifest is derived twice from production and then executed. The first executable 1,000-entry probe found **152 refusals** in the supposedly reachable 5,060-entry base: the manifest passed `node_id` to a curriculum predicate keyed by DNA, while every production caller passed the DNA. Current independently agreeing production routing has **455 node/DNA/formatter pairs, 4,269 base obligations, and 18,762 continuous class crossings** after four incompatible `peso_money_build` routes were removed. The finite executor crosses every base with 27 interest requests (26 names plus automatic selection) and four production experience wrappers: **461,052 finite obligations**. Release uses five deterministic seed slots, with min/interior/max continuous representatives in the first three: 576,315 cached generator/formatter calls representing **2,305,260 executions**. The cache is only across the terminal experience step; all four wrappers execute on deep copies. PR is explicitly partial: fixed sentinels span every node, DNA, formatter, interest request, experience and seed slot, plus every base key supplied as changed. A digest-bound four-worker benchmark executes 1,000 cache keys / 4,000 represented obligations with zero failures and must project below four hours with every recommended shard below 30 minutes; exact current timings and RSS live in `validation_reports/phase2_hardening/obligation_benchmark.json` so recording them does not itself invalidate the digest. Release execution has a bounded work queue, reports progress every 1,000 completed cache keys, and stops submitting work on the first named failure. Release receipts are mandatory: absence, incompleteness, overlap, staleness, failures, or an over-budget shard is a named failure, and only the complete current modulo-shard union passes. Five registered formatter routes remain unreachable at a shrink-only floor. BLIND SPOTS: continuous axes move together, so their Cartesian interactions and behavior between representatives are unproved. `None` interest is automatic seeded selection, not neutral, and out-of-band named themes also fall back automatically; whether an ignored request is acceptable belongs to H-05. Two derivations still share production tables and can share a misunderstanding | `validate_obligations` §11 `obligation_derivations_agree`, `obligation_routes_reachable`, `obligation_dimension_coverage_11`, `obligation_execution_11`, `obligation_benchmark_11`, `obligation_release_shards_11` | local `run_all`; release executor is explicit |
| Every SILENT exception handler in the validation package carries a recorded disposition, in a comment inside the handler: `# DISPOSITION: checked | named-failure | limitation -- <why>`. Silent means the body does nothing but leave — `continue`, `pass`, or a bare `return`, and nothing else; a handler that records a finding and THEN continues is the named-failure pattern this exists to encourage, not an instance of the defect. **Plan step 0's inventory, and a HARD ZERO rather than a floor.** The original 19 live handlers were classified before activation (9 `checked`, 3 `named-failure`, 7 `limitation`); H-05 then removed two checked catch-and-drop paths from `validate_dna`, leaving **17** (7 `checked`, 3 `named-failure`, 7 `limitation`) on 2026-09-14. The disposition lives in the code rather than in a table keyed by file and line, because such a table rots on the first edit above it; this travels with the code it describes and is greppable. The first definition found 33; the 14 it stopped flagging all record a finding first, §10's own two obligation reporters among them. One real defect was fixed rather than marked while doing this: `validate_interest` answered a DNA `ImportError` with a bare `continue`, so a broken DNA module vanished from §4 while the stage still printed `12/12 passed`. BLIND SPOTS: a marker is a claim, not a proof — `checked -- §1G covers this` asserts coverage, nothing here verifies it; scope is the validation package only, not the pipeline; and a handler that logs and continues is not counted, though it can still lose an obligation | `validate_coverage` §8 `silent_path_disposition_8` | local `run_all`; not in CI |
| A judgment packet's sample allocation covers every declared `(variant, value)` pair with **no per-node cap**, pins **interest** explicitly, varies continuous difficulty across **more than two points**, and varies **experience**. Measured 2026-09-12, before → after: variant pairs reachable **683 → 975** (+292 exactly as planned; 68 nodes had exceeded the cap of 6, and §7's census floor counts CANDIDATES rather than coverage, so nothing could notice 30% of the declared variant space was permanently unreviewable); interest allocation **0 → 3 buckets** per node (a themed interest, a contrasting theme, the neutral default — the string `interest` appeared **zero times** in the packet builder although `pipeline.run()` has always accepted `student_interest`, so every review ever filed described the neutral default only); continuous difficulty **2 → 4 kinds** (default, all-axes ceiling, ONE axis at a time so "which axis made this too hard" is answerable, and the floor); experience **never varied → every wrapper but the default**. Tree-wide: **3,514 samples, mean 23.3, min 17, max 44**. Each new dimension has its own reserved seed range because freshness re-renders from `(node_id, seed)` with no profile argument, and samples additionally carry an explicit `requested` block so a new record does not depend on seed arithmetic alone to describe itself. **MEASURED, NOT GATED:** of 975 declared pairs, **849 land as distinct samples and 126 render text identical to a sample already in the packet** — a declared variant the DNA never reads. Zero render failures tree-wide. §2I gates the *unproducible* direction; the *no-op* direction is reported here and stays judgment work. **NAMED LIMIT:** the groups are MARGINAL, not CROSSED — a visual formatter at max difficulty with a pinned variant under a themed interest is still only sampled by chance, and the packet must not be read as demonstrating joint coverage it does not have | `tests/unit/test_packet_allocation.py` (§0) | local `run_all`; not in CI |
| The suite's own census (nodes, unit tests, mutations, variant candidates) has not shrunk below its floor. Ratcheted to the real numbers 2026-09-08: unit_tests 340→395 (399 collected, so 59 tests could have stopped running unnoticed) and mutations 37→48; 2026-09-11: unit_tests 419→425 (428 collected) and mutations 72→76, the four that prove the three media built that day | `validate_census` §7 | local `run_all`; standalone in seconds |
| An Attester verdict must name who made it (`attested_by`), and no identity may cover more than 25 nodes — mirroring §5's reviewer plurality. Measured 2026-08-28: all 173 existing records carried NO identity field, so independence was uncheckable on a surface 4x larger than §5. Records before 2026-08-28 are grandfathered; failing them would condemn genuinely blind work to close a schema gap | `validate_capability` §6H (**Phase 2**) | local `run_all` **Phase 2**; not in CI |
| A rendered visual payload must depict something real and agree with its own answer — no invented currency, no empty grids, no value off its own axis, and (2026-09-11) no run of equal jumps that lands anywhere but on the value the item keys. The jumps clause is stated over the payload's own fields, so any grade's number line that declares them inherits it; it checks a DECLARED run, not that a competency naming the medium gets one — that absence is gated by `tests/unit/test_media_the_competency_names.py`, because the component reads the jump keys inside a branch and §9 enforces unconditional keys only | `validate_matrix` §1G | local `run_all`; not in CI |
| The harness's own unit tests pass before any stage reports green — green stages over red tests is not evidence | `pytest tests/unit` §0 | local `run_all`; not in CI |
| A capability's `bounds` provider must be a numeric-ceiling claim about that capability, never a list most of the table carries verbatim. **Phase 1** | `validate_capability` §6E | local `run_all` **Phase 1**; not in CI |
| Every declared capability carries a blind Attester verdict that the rendered output exhibits what its clause names, and no entry contradicts one. **Phase 2**: reads `validation_reports/attestation/`, so it cannot run until an agent has filed one | `validate_capability` §6F | local `run_all` **Phase 2**; not in CI |
| An attestation stops being evidence when the content it judged changes: every record still supplying a winning verdict is re-rendered and must still match on the same four things §5 compares — stem, the `options` it was shown, the keyed **value** resolved through those options, and the offered option multiset (brought to §5 parity 2026-09-10; it compared the STEM ONLY before). Measured that day across 151 live records: 5 stale on the stem, **137 carrying no `options` at all** and so unadjudicable — the root defect was the record skeleton in `tests/attester_packets.py`, which printed options to the Attester and then wrote 4 fields without them (0 of 1790 recorded samples had one); fixed in the same commit, but the 137 already filed cannot be repaired and must be re-attested. The example this gate was justified by — "`b11_mat_g2_na_q3_5` was attested against `False` and now renders an empty key" — was itself `_normalize(False)` returning `""`; that seed still keys `False`, and `_answer_value` now stops the falsy collapse for both §5 and §6F. **Not compared**: the visual payload, which is exactly what a medium clause is attested for. A record is exempt only once **every** one of its verdicts has been replaced by a later record — supersession is derived from the records themselves, never from a record's own `supersedes` text | `validate_capability` §6F freshness (**Phase 2**) | local `run_all` **Phase 2**; not in CI |
| An attestation must show its work: its reasoning is present and not a per-clause fill-in of one shared sentence frame (max 3 verdicts per normalized skeleton), a `PROVIDED` verdict names the seeds that show the clause, every named seed exists in that record's own `packet.samples_judged`, and no record carries more than one blind dispatch (25 verdicts). Judged on live verdicts only, by the same last-file-wins supersession rule §6F freshness uses | `validate_capability` §6G (**Phase 2**) | local `run_all` **Phase 2**; not in CI |

**On "local `run_all`; not in CI".** Every rule above is still enforced and still fails loudly — but
the enforcement now runs **only** where someone runs it: `python -m backend.app.practice_gen.validation.run_all`,
executed by the hardening loop each tick and by anyone touching the pipeline. As of 2026-08-12 no
GitHub Actions workflow runs it.

The reason is that `run_all` exits 0 only at a 100% node PASS rate, which makes it a *done* signal for
the curriculum work, not a shipping criterion. Wiring it to CI meant a curriculum verdict about a
Grade-2 word problem blocked unrelated backend deploys, including the manual testing by which those
verdicts get resolved.

Nothing here was weakened to make a build pass — no assertion changed, no check relaxed, no `|| true`
introduced. What changed is *where* the checks run, and that carries a real cost worth naming: a rule
in this table is now only as binding as the discipline of running the harness. There is no longer an
automated tripwire that stops an unverified pipeline change from reaching production. Restoring one
once the census reaches zero is the obvious fix; until then, `run_all` before you push is the contract.

**Known blind spot — the matrix report is overwritten by a single-node run (named 2026-08-26).**
Every matrix rule above (§1A through §1H) is enforced by `validate_matrix`, which writes its results to
`validation_reports/matrix_report.json`. Running it for one node —
`validate_matrix --node mat_g1_na_q1_0` — **replaces** the tree-wide report with that one node
rather than merging into it. Any consumer reading that file afterwards sees a tree of one.

This matters because the hardening supervisor reads that report instead of re-running a
30-minute matrix sweep every tick. The mitigation is in the reader, not the writer:
`hardening_supervisor.matrix_evidence()` compares the report's node coverage against
`get_all_node_ids()` and its mtime against the newest file under `backend/app/practice_gen/`
and `data/skeletons/`, and marks anything incomplete or outdated as `PARTIAL` / `STALE` /
`MISSING` / `UNREADABLE`. Only a `FRESH` report contributes its count to the work queue;
every other state is reported as *unmeasured* and forces a refresh. Proven by
`tests/unit/test_supervisor_queue.py`, which plants each state and asserts it is refused.

Merging instead of clobbering is *not* obviously the better fix, and it is worth saying why.
A naively merged report would carry results produced at different times against different
generator revisions, then present the union as tree-wide coverage — mixed freshness reported
as a clean sweep, which is a quieter failure than the clobber. A merge is only safe if every
node's row carries its own timestamp and freshness is judged per row. That is unbuilt.

Until it is: **a single-node matrix run leaves the tree without stage-6 evidence**, the reader says
so rather than guessing, and the next full `run_all` restores it. `tests/mutation_harness.py`
snapshots and restores the report around its run for the same reason — most of its mutations
invoke `validate_matrix --node X`, so without that the act of proving the harness would have
destroyed the evidence the harness depends on.

**Why the queue counts stages 6, 7 and 8 (2026-08-26).** The supervisor's RESUME/NOTHING_TO_DO
verdict previously derived from `validate_capability` alone — stage 8 of eight. It could therefore
report "no work" with 575 stale reviews (stage 7) and a live `empty_execution_matrix` (§1C-coverage, stage 6)
outstanding, and for 455 consecutive ticks it reported a 158-item queue that was really 735. The
gate now sums all three bands, and `scripts/measure_queue.py` prints them from that one measurement
so the tick protocol cannot compute a second, divergent number.

## Core Principles

1. **Matatag Lab as Single Source of Truth**: The Lab's Generate Preview must render exactly what the student portal will serve for the same enabled options. Drop `is_lab=True` for normal previews so the Lab runs through the same competency-bound clamp.
2. **Avoid Graceful Fallbacks**: The pipeline must fail fast and loud when schema validation, import, or limits are violated. No silent defaulting behavior is allowed.
